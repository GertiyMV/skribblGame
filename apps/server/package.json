{
  "name": "@skribbl/server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "@skribbl/shared": "file:../../packages/shared",
    "redis": "5.11.0",
    "socket.io": "4.8.3",
    "zod": "4.3.6"
  },
  "scripts": {
    "lint": "eslint \"src/**/*.ts\"",
    "type-check": "tsc --noEmit -p tsconfig.json",
    "test": "tsx --test-isolation=none --test \"src/**/*.test.ts\"",
    "build": "tsc -p tsconfig.json && node ./scripts/copy-word-dictionaries.mjs"
  },
  "devDependencies": {
    "pino": "^10.3.1"
  }
}
