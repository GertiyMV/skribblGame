{
  "name": "@skribbl/server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "@skribbl/shared": "file:../../packages/shared"
  },
  "scripts": {
    "lint": "eslint \"src/**/*.ts\"",
    "type-check": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"Tests for server are not implemented yet\"",
    "build": "tsc -p tsconfig.json"
  }
}
